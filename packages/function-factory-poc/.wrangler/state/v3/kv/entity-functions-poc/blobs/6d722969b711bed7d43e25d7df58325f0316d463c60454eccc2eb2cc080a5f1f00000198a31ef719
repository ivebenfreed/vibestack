
      function query(filters = {}) {
        // This would generate SQL or KV queries
        // For POC, just return the filter structure
        return {
          table: 'storage-test-org_storagetestentitys',
          filters: filters,
          coreFields: ["id","name","description","status","created_at","updated_at"],
          customFields: ["testField"]
        };
      }
      
      return query(filters);
    